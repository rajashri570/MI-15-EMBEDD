Commands:

$man git
	its prvide the mannal of git

$git config --global user.name "Your Name Comes Here"
	Enter the username

$git config --global user.email "you@yourdomain.example.com"
	Enter the mail id 

$git init	
	Initialized empty Git repository in .git

$ git add . FileName.txt
	to add Files on git.

$ git commit
	This snapshot is now stored in a temporary staging area which Git calls the "index". 
	You can permanently store the contents of the index in the repository with git commit.

$ git add file1 file2 file3
	To add Multiple Files on git.
	
$ git diff --cached
	Without --cached, git diff will show you any changes that you’ve made but not yet added to 		the index.
	
$ git status
	You can also get a brief summary of the situation with git status.

$ git commit
	If you need to make any further adjustments, do so now, and then add any newly modified 		content to the index. Finally, commit your changes with git commit.
	
$ git log
	At any point you can view the history of your changes using.

$ git branch
	It showa the  list of all existing branches.
	
$ git pull
	he git pull command is used to fetch and download content from a remote repository and 		immediately update the local repository to match that content.
		
sudo mkdir
	make a new diretory.
	
$sudo rmdir DirName
	Delete a Empty Directory.
	
$sudo rmdir -r DirName		
	Delete a non Empty string.

$sudo touch 
	create a new file.
	
$mv
	move file from one source to another source.


	
	
	


